# Handoff — 2026-04-10 세션 20

세션 19 (알람 묶음 + 공지 리팩토링) 다음 세션. **핵심: 만족도 통계 누락 문항 추가 + v8 프롬프트 3건 구현 + 더미 재작성.**  다음 세션(21)은 **통합 브라우저 테스트 + SMS 도입**.

---

## 1. 세션 20 에서 끝난 것

### A. 만족도 조사 통계 누락 문항 추가

**배경**: 설문 폼에 있지만 어드민 통계 탭에 없던 문항 6개 발견.

**`src/lib/survey.ts`**
- `SurveyStats` 인터페이스에 6개 필드 추가:
  - `pastReligion` (Q2 — 1년 전 종교)
  - `religionInfluencePersonal` (Q3 — 개인 삶 영향, 4점)
  - `religionInfluenceSociety` (Q3-1 — 사회 영향, 4점)
  - `pastParticipation` (Q4 — 과거 참여 경험)
  - `decisionMaker` (Q5 — 참여 결정자)
  - `expectation` (Q7 — 기대한 부분)
- `calcSurveyStats` 에서 위 6개 수집 및 `countBuckets` 집계

**`src/pages/admin/stats/StatsSurveyTab.tsx`**
- 응답자 정보 섹션에 Q2 (1년 전 종교) BucketTable 추가
- "3. 종교 영향력" 섹션 신규 (Q3, Q3-1)
- "4~7. 참여 동기" 섹션 신규 (Q4, Q5, Q7)

### B. Q11-1/Q11-2 서술형 콘텐츠 미표시 버그 수정

**원인**: `.openList` CSS — `flex: 1 1 0; min-height: 0` 이 `.openBlock` flex 컨테이너 안에서 높이를 0으로 축소.

**수정**: `flex: 1 1 0; min-height: 0` 제거 → `max-height: 400px; overflow-y: auto` 로 변경. Q20 개선 의견도 동일 적용.

### C. v8 프롬프트 구현 3건

#### C-1. 쿠폰 중복 방지 — localStorage 기반

**`src/lib/survey.ts`**
- `hasSurveyDoneLocally()` / `markSurveyDoneLocally()` — `localStorage.survey_done` 플래그
- 시크릿 모드 등 localStorage 불가 시 조용히 무시

**`src/pages/sections/survey/SurveyForm.tsx`**
- 진입 시 `hasSurveyDoneLocally()` 체크 → true 면 "이미 설문에 참여하셨습니다" 화면 + 음식 주문 CTA
- 제출 성공 후 `markSurveyDoneLocally()` 호출

#### C-2. 부스 알람 단순화

**변경 전**: 새 주문 즉시 알람 + 1분/2분 초과 전이 감지 (주문별 alertedIdsRef)
**변경 후**: 미확인 주문 1개 이상 존재 시 **1분 간격** `order_alarm.mp3` 3회 재생 + 진동

**`src/lib/audioCue.ts`**
- `CueType` (`'alarm' | 'alert'`) 제거 → `playSound(repeat)` 단일 시그니처
- `order_alert.mp3` 참조 제거 (파일은 `public/sounds/` 에 잔존, 코드 미사용)

**`src/pages/booth/BoothDashboardPage.tsx`**
- `onOrderPaid` 에서 즉시 `playSound`/`vibrateSafe` 제거
- `alertedIdsRef` + 1분/2분 전이 감지 useEffect 전부 제거
- 신규: `ALARM_INTERVAL_MS = 60000` → `setInterval` 로 미확인 주문 존재 여부 체크 후 알람
- `ALERT_SECONDS = 60` 유지 — 카드 시각 경고(빨강/펄스)용. 소리와 무관

#### C-3. OrderStatusPage 조리완료 스트립

**`src/pages/OrderStatusPage.tsx`**
- `dismissedBooths` state (Set) — 확인 클릭 시 해당 부스 스트립 숨김
- `readyBooths` memo — `ready_at` + `status !== 'cancelled'` + 미dismiss 부스 중복 제거
- 컨테이너 최상단에 스트립 렌더 (매장명 + "준비완료 · 픽업해주세요" + ✓ 버튼)
- Realtime 으로 `ready_at` 변경 감지 시 자동 추가

**`src/pages/OrderStatusPage.module.css`**
- `.readyStrip` / `.readyStripText` / `.readyStripBtn` 신규

### D. 더미 데이터 재작성

**`_DEV/Seeds/20_seed_surveys_dummy.sql`** — 5건 → 10건 전면 재작성

수정 사항:
- `q2`: 숫자 문자열 (`"3"`) → 종교 코드 (`"buddhism"`, `"none"` 등)
- `q3_1`: 빈 문자열 → 4점 척도 (`"1"`~`"4"`)
- `phone`: 하이픈 제거 (`01000000001` 형식, phone 통일 규칙 준수)
- DELETE 문으로 기존 5건(하이픈 포함) 정리 후 삽입
- q11 분포: 불만족(2), 보통(3~4), 만족(5~6), 매우만족(7)
- 종교 다양화: 불교, 개신교, 천주교, 유교, 다른 종교, 무교
- q7 (종교 변경): 1년 전과 현재 종교 다른 사례 포함 (응답7: none→protestant)

---

## 2. 핵심 결정사항

| 항목 | 결정 |
|---|---|
| **누락 통계** | Q2, Q3, Q3-1, Q4, Q5, Q7 — BucketTable 로 추가 |
| **서술형 표시** | `.openList` max-height 400px 스크롤 (Q11-1/Q11-2/Q20 공통) |
| **설문 중복 방지** | phone(DB) + localStorage(디바이스) 이중 방어 |
| **부스 알람** | 즉시/1분/2분 구분 폐기 → 미확인 주문 존재 시 1분 간격 단일 알람 |
| **알람 사운드** | `order_alarm.mp3` 단일. `order_alert.mp3` 미사용 |
| **조리완료 안내** | OrderStatusPage 상단 스트립, 클라이언트 dismiss (DB 저장 없음) |
| **더미 데이터** | 10건 재작성, phone 하이픈 없음, q2/q3_1 정상 값 |

---

## 3. 변경 파일 (세션 20)

### 수정 — 통계 누락 + 서술형 버그
- `src/lib/survey.ts` — SurveyStats 6필드, calcSurveyStats 집계, localStorage 헬퍼
- `src/pages/admin/stats/StatsSurveyTab.tsx` — 3개 섹션 추가 (Q2, Q3/Q3-1, Q4/Q5/Q7)
- `src/pages/admin/stats/StatsSurveyTab.module.css` — openList flex→max-height

### 수정 — v8 프롬프트
- `src/lib/audioCue.ts` — playSound 단일 시그니처
- `src/pages/sections/survey/SurveyForm.tsx` — localStorage 중복 방지
- `src/pages/booth/BoothDashboardPage.tsx` — 알람 단순화 (1분 간격)
- `src/pages/OrderStatusPage.tsx` — 조리완료 스트립
- `src/pages/OrderStatusPage.module.css` — 스트립 CSS

### 수정 — 더미
- `_DEV/Seeds/20_seed_surveys_dummy.sql` — 10건 재작성

### 핸드오프
- `_DEV/Handoff/food_payment_prompt_v8_fixes.md` — v8 원안
- `_DEV/Handoff/2026-04-10_세션20_핸드오프.md` — 본 문서

### 커밋 (세션 20)
- `a8f6813` — Add missing survey stats (Q2~Q7) and fix open comment visibility
- `c4ef343` — Add localStorage survey guard, simplify booth alarm, and add ready pickup strip
- `f28973f` — Rewrite survey dummy data (10 entries) and clean up booth comment

---

## 4. 머신 시작 체크리스트

```bash
# 1. dev 브랜치
git pull origin dev
# 커밋: f28973f

# 2. 의존성 — 신규 없음
npm install

# 3. DB — 더미 재작성 SQL 실행 (선택)
#    _DEV/Seeds/20_seed_surveys_dummy.sql
#    기존 더미 DELETE 포함이라 SQL Editor 에서 그대로 실행

# 4. env 확인 — 변경 없음

# 5. 빌드
npm run build

# 6. 로컬 dev
vercel dev
```

---

## 5. 미해결 / 다음 세션 (세션 21)

### 5.0 — 통합 브라우저 테스트 (0순위, 세션 19~20 이월)

세션 19 핸드오프 5.0 체크리스트 + 세션 20 추가분:

**추가 테스트 항목 (세션 20)**
- [ ] 설문 제출 성공 → localStorage `survey_done=true` 확인
- [ ] 같은 기기에서 설문 재진입 → "이미 참여하셨습니다" 화면
- [ ] 시크릿 모드에서 설문 → localStorage 없이 정상 동작
- [ ] 부스 대시보드: 미확인 주문 있을 때 1분 간격 알람 재생 확인
- [ ] 부스 대시보드: 모든 주문 확인 후 알람 중단 확인
- [ ] 부스 대시보드: 카드 시각 경고 (60초 초과 빨강) 유지 확인
- [ ] OrderStatusPage: 부스 준비완료 시 상단 스트립 자동 출현
- [ ] OrderStatusPage: ✓ 클릭 → 해당 스트립만 사라짐
- [ ] OrderStatusPage: 여러 부스 준비완료 → 스트립 여러 줄
- [ ] 통계 탭: Q2, Q3, Q3-1, Q4, Q5, Q7 정상 집계 (더미 10건 기준)
- [ ] 통계 탭: Q11-1/Q11-2 서술형 내용 표시 확인
- [ ] 통계 탭: Q20 5건 이상 시 스크롤

### 5.1 — 알리고 SMS 도입
세션 18 부터 계속 이월.

### 5.2 — 부스 거절 → 손님 SMS
SMS 도입 이후.

### 5.3 — AdminNotices CSS cleanup
마크다운 잔재 일괄 삭제.

### 5.4 — react-markdown 의존성 정리
`npm uninstall react-markdown`.

### 5.5 — 어드민 알람 소리 옵션
운영 후 결정.

### 5.6 — 어드민 로그인 분리 + dev→main 머지

### 5.7 — order_alert.mp3 정리
`public/sounds/order_alert.mp3` 파일 삭제. 코드 참조는 세션 20 에서 이미 제거.

---

## 6. 회귀 점검

✅ 빌드 통과 (`npm run build`)
✅ 타입체크 통과 (`npx tsc --noEmit -p tsconfig.app.json`)
✅ 기존 기능 (phone/쿠폰/알람/공지) 코드상 회귀 없음

---

## 7. 다음 세션 유의사항

1. **더미 SQL 실행**: 세션 20 더미 재작성 SQL 을 Supabase SQL Editor 에서 실행해야 통계 테스트 가능
2. **iOS Safari unlockAudio**: 부스 알람 단순화 후에도 BoothLoginPage 의 `unlockAudio()` 는 유지. 로그인 → 대시보드 → 1분 뒤 자동 알람이 iOS 에서 재생되는지 실기 테스트
3. **localStorage 초기화**: 설문 테스트 시 DevTools → Application → Local Storage 에서 `survey_done` 삭제하면 재진입 가능
4. **조리완료 스트립**: dismiss 는 클라이언트 상태만 — 페이지 새로고침 시 다시 표시됨 (의도된 동작)
