# Handoff — 2026-04-09 세션 17

세션 16 다음 세션. **핵심: Heroicons → Lucide 전환, 공지사항 게시판, 만족도조사 관리 탭, 어드민 사이드바 재구성, 네이비 액션 버튼.** 다음 세션(18) 0순위는 **만족도조사 대상자 자동 쿠폰 발급 시스템**.

---

## 1. 세션 17 에서 끝난 것

### A. Heroicons → Lucide 마이그레이션 (27 파일)

- `@heroicons/react` 제거 → `lucide-react` (`lucide-react@1.8.0`) 설치
- BottomNav 선행 교체 후 `/tmp/lucide-codemod.mjs` 로 25 파일 일괄 교체
- 이름 매핑 주요:
  - `PhotoIcon` → `Image as ImageIcon` (HTMLImageElement 충돌 회피)
  - `RectangleGroupIcon` → `LayoutGrid`
  - `CheckCircleIcon` → `CircleCheck`, `XCircleIcon` → `CircleX`
  - `ExclamationTriangleIcon` → `TriangleAlert`
  - `ArrowRightOnRectangleIcon` → `LogOut`, `NoSymbolIcon` → `Ban`
  - `ArrowPathIcon` → `RotateCw`, `MagnifyingGlassIcon` → `Search`
  - `ChartBarSquareIcon` → `ChartColumn`, `ReceiptRefundIcon` → `ReceiptText`
- **글로벌 기본 stroke-width**: `src/styles/theme/global.css` 에 `svg.lucide { stroke-width: 0.9 }` (초기 1.5 → 1.0 → 0.9 로 확정)
- **BottomNav**: outline+solid 이원화 제거, `activeIcon` 배열 제거
  - active 상태: CSS `transform: translateY(-2px) scale(1.2)` + 컬러 `--color-primary`
  - stroke-width override 제거 (글로벌 0.9 사용)
  - 비활성 컬러 `#737373`
  - 초기에 여러 옵션(scale / lift / bounce / circle bg) 실험했다가 최종 옵션3 (lift + scale) 로 안착
- 메모리 갱신 필요: Heroicons 룰 → Lucide 룰 (TBD, 핸드오프에만 기록)

### B. 공지사항 게시판 (손님 + 어드민)

**DB**: `notices` 테이블 + RLS 기존 존재. 스토리지 버킷 `festival-assets` 의 `notices/` 경로 재사용.

**lib** `src/lib/notices.ts` 신규
- `fetchAllNotices()`, `fetchPublishedNotices(offset, limit)`, `fetchPublishedNoticeById(id)`
- `createNotice(input)`, `updateNotice(id, input, prev)`, `deleteNotice(id)`
- `uploadNoticeImage(file)` — 이미지 업로드 + public URL 반환 (마크다운 `![](url)` 삽입용)
- `publish false→true` 때만 `published_at` 자동 스탬프

**어드민** `src/pages/admin/AdminNotices.tsx`
- 테이블: #/제목(📌 표시)/카테고리/상태/작성일/발행일/삭제
- 15개 페이지네이션 (`Pagination` 재사용)
- 통계 박스 3개 (발행/초안/고정)
- 행 클릭 → 수정 모달, 상단 "새 공지" → 작성 모달
- 모달: 제목 · 카테고리(일반/프로그램/결과) · 상단고정 토글 · 발행 토글 · 마크다운 textarea · 이미지 삽입 버튼 (커서 위치에 `![](url)` 자동 삽입)

**손님**
- `src/pages/sections/NoticeSection.tsx` — 미니멀 목록 (제목 + 날짜 + 📌 아이콘)
  - 5개 배치 무한스크롤 (IntersectionObserver, rootMargin 200px)
  - 고정글 상단 정렬, 카테고리 탭 없음
- `src/pages/NoticeDetailPage.tsx` 신규 — 뒤로가기 + 제목 + 날짜 + react-markdown 본문
- `src/App.tsx` — `/notice/:id` 라우트 추가
- **의존성 추가**: `react-markdown`

### C. 테마 컬러 통일 (23 파일)

하드코딩된 구 그린 → 토큰으로 일괄 치환:
- `#1E3A2F` → `var(--color-primary)` (56회)
- `#142820` → `var(--color-primary-dark)` (16회)
- `rgba(30,58,47,X)` → `color-mix(in srgb, var(--color-primary) X%, transparent)`
- `#1E7F3A`, `#2F6B4A` 변종 그린 및 관련 rgba 모두 정리

대상: 어드민 13 파일 + 공용 ui 5 파일 + 손님 sections 5 파일 = 23 파일. 현재 `#1E3A2F` 등 스테일 그린 **0**.

`/tmp/theme-codemod.mjs` 스크립트로 일괄 replace.

### D. 만족도조사 관리 탭 + 어드민 사이드바 재구성

**대시보드 제거**: `src/pages/admin/AdminDashboard.tsx` 삭제. `AdminDashboard.module.css` 는 유지(AdminRevenue/AdminSurvey 재사용).

**사이드바 운영 그룹 새 순서**:
1. 공지사항 관리 → `/notices` (Megaphone)
2. 참가신청 관리 → `/applications` (FileText)
3. 쿠폰 관리 → `/coupons` (Ticket)
4. 매출 관리 → `/revenue` (ChartColumn) — 신규 `AdminRevenue.tsx`
5. 만족도조사 관리 → `/survey` (ClipboardList) — 신규 `AdminSurvey.tsx`

**App.tsx**: index 라우트 → `/notices` 리다이렉트, 404 fallback 도 `/notices`, 로고 클릭 → `/notices`.

**신규 탭** `src/pages/admin/stats/StatsSurveyTab.tsx`
- 날짜 필터 없음 — 누적 집계만 (상단 "누적 응답 집계 · 총 N명" + 새로고침 버튼)
- 핵심 KPI 5개: 총 응답수 / 평균 연령 / 최다 거주지역 / 전반 만족도 / 종합 만족도
  - 그리드 `1fr 0.85fr 1.7fr 0.85fr 0.85fr` (최다 지역 넓게 — 지역 라벨 잘림 방지)
- 카드 순서 (문번호 순):
  1. 핵심 KPI
  2. 응답자 정보 (성별/연령/지역/종교/종교시점/종교빈도)
  3. 문6 정보 출처 (복수선택)
  4. 문8~10 행사 평가 (리커트 그리드)
  5. 문11 종합 만족도 (2열 KPI + Q11-2/Q11-1 주관식)
  6. 문12~16 행사 운영
  7. 문17~18 의향 및 성과 (리커트 그리드)
  8. 문19 + 문20 (2열 `dualSectionGrid` — 20번 카드 넘치면 스크롤)
  9. 원본 응답 목록 (페이지네이션 + 상세 모달)
- 상세 모달: 780px max-width, 3:2 질문:응답 그리드, 질문 nowrap + ellipsis

**lib/survey.ts 확장**
- `fetchSurveys(filters)`, `calcSurveyStats(rows)`, `todayKstString()`
- **작년 공식 동일**: `to100Avg = avg / max * 100` (기존 `(avg-1)/(max-1)*100` 아님 — 세션 중 수정)
- `topBoxRatio7` = 5,6,7점 비율 (7점 척도)
- `SurveyStats` 에 `avgAge`, `topRegion` 추가
- 라벨 단일 진실원: `@/pages/sections/survey/questions` 에서 모든 옵션 import → `SURVEY_LABELS` / `SURVEY_ITEMS` 재export
- 운영(Q12~16) 라벨도 폼의 전체 질문 텍스트 그대로

**더미 데이터**: `_DEV/Seeds/20_seed_surveys_dummy.sql` — 5건 (다양한 성별/연령/지역/종교, Q1~Q20 전항목 포함). SQL Editor 에서 수동 실행.

### E. 네이비 액션 버튼

`#2563EB` / `#1D4ED8` → `#283648` / `#17202E` (채도 빠진 네이비, 한 번 더 진하게 조정):
- `AdminCoupons.module.css` — 수동발급 버튼
- `AdminNotices.module.css` — 새 공지 버튼
- `AdminMonitor.module.css` — modalItemConfirmBtn (주문 확인 버튼), disabled 색상도 `#94A3B8` 로 같이 변경

차트 그라디언트 (StatsRevenueTab), 카테고리 컬러칩 (AdminFood/FoodSections) 은 건드리지 않음.

### F. 작은 이슈 수정

- 세션 중 사용자 지적 사항 반영:
  - 응답자 정보 카드 높이 통일 (`bucketCard { min-height: 260px }`, `grid-auto-rows: 1fr`)
  - 8~10 리커트 줄간격 축소 (`gap 8→2, min-height 56→36`)
  - 19~20 2열 배치 (`dualSectionGrid`)
  - 날짜 포맷 — AdminOrders 의 `mm/dd hh:mm` (KST) 재사용
  - Q18 같은 긴 질문도 `white-space: normal` 로 자동 줄바꿈 (min-height 로 기본 높이 유지)

---

## 2. 핵심 결정사항

| 항목 | 결정 |
|---|---|
| **아이콘 라이브러리** | Lucide (stroke-width 0.9 글로벌) |
| **공지사항 본문** | Markdown (react-markdown), 이미지는 `![](url)` + 업로드 버튼으로 커서 위치 삽입 |
| **만족도조사 탭 필터** | 날짜 필터 없음 · 누적 집계만 |
| **작년 공식** | `avg / max * 100` (작년 분석 엑셀과 동일) · top-box = 5~7점 비율 |
| **만족도조사 라벨** | 설문 폼(`questions.ts`) 을 단일 진실원으로 import |
| **대시보드** | **제거** — 탭은 각각 사이드바 항목으로 승격 |
| **네이비 버튼** | `#283648` / hover `#17202E` — 쿠폰/공지/모니터 동일 |
| **어드민 날짜 포맷** | `AdminOrders` 의 `mm/dd hh:mm` (KST) 로 통일. 자체 포맷터 금지 (메모리에 저장됨) |

---

## 3. 변경 파일 (세션 17 최종)

### 신규
- `src/lib/notices.ts`
- `src/pages/NoticeDetailPage.tsx` + `.module.css`
- `src/pages/admin/AdminNotices.tsx` + `.module.css`
- `src/pages/admin/AdminRevenue.tsx`
- `src/pages/admin/AdminSurvey.tsx`
- `src/pages/admin/stats/StatsSurveyTab.tsx` + `.module.css`
- `_DEV/Seeds/20_seed_surveys_dummy.sql`

### 삭제
- `src/pages/admin/AdminDashboard.tsx` (대시보드 페이지 제거)

### 수정 (주요)
- `src/lib/survey.ts` — fetchSurveys/calcSurveyStats/todayKstString 추가, 공식 수정
- `src/App.tsx` — Notices 상세, Revenue, Survey 라우트
- `src/components/admin/AdminLayout.tsx` — 사이드바 재정렬, 로고 네비게이션
- `src/components/layout/BottomNav.tsx` + `.module.css`
- `src/styles/theme/global.css` — Lucide 글로벌 stroke-width
- 26 파일 Heroicons → Lucide 교체 (코드몹)
- 23 파일 구 그린 → 토큰 (코드몹)
- `AdminCoupons/AdminNotices/AdminMonitor.module.css` — 네이비 버튼

### 커밋
- `b92cfac` Migrate icons from Heroicons to Lucide
- `ee2826c` Build notice board and unify admin/booth theme to steel blue
- `77e0424` Add satisfaction survey admin tab and split dashboard into sidebar
- `3f22e7a` Tune survey KPI grid, detail modal, and navy button color

---

## 4. 머신 시작 체크리스트

```bash
# 1. 최신 main
git pull origin main
# 최신: 3f22e7a "Tune survey KPI grid, detail modal, and navy button color"

# 2. 의존성 — lucide-react, react-markdown 신규
npm install

# 3. env 확인 — 변경 없음 (기존 Supabase/Toss 그대로)

# 4. DB — 기존 surveys 테이블 그대로. 시드 수동 실행:
#    _DEV/Seeds/20_seed_surveys_dummy.sql 을 Supabase SQL Editor 에서 실행

# 5. 빌드
npm run build

# 6. 로컬 dev
vercel dev      # 또는 npm run dev
# http://localhost:3000 (손님) / admin.localhost:3000 (어드민) / booth.localhost:3000 (부스)
```

---

## 5. 미해결 / 다음 세션 (세션 18)

### 5.0 — **만족도조사 대상자 자동 쿠폰 발급 시스템 (0순위, 사용자 직접 요청)**

**배경**:
- 현재 `surveys` 테이블에 응답은 쌓이지만 쿠폰 자동 발급 로직이 없음 (세션 15/16 핸드오프부터 계속 백로그)
- `coupons` 테이블 이미 존재, 수동 발급은 구현됨 (`AdminCoupons` 의 `createCouponManually`)
- 설문 제출 시점에 자동으로 쿠폰 1장 발급 → 응답자 폰/이메일로 통지 또는 코드 즉시 노출

**설계 포인트 (세션 18 초입에 결정)**:

1. **발급 트리거**
   - (a) 클라이언트 `submitSurvey` 내부에서 쿠폰 insert 를 같이 (트랜잭션 / 2단계)
   - (b) Supabase DB trigger (surveys insert → coupons insert)
   - (c) Edge function (설문 제출 후 fetch)
   - **(a) 추천** — 프론트에서 명시적으로 insert + UX 제어 쉬움. 실패 시 retry 가능. DB trigger 는 롤백/테스트 난해.

2. **쿠폰 속성**
   - 할인액: 2,000원 / 3,000원 (기획 확인 필요)
   - 최소 주문액: 10,000원 (쿠폰관리 수동 발급 기본값과 동일?)
   - 만료: 축제 마지막날 23:59:59 KST (활성 festival 조회 필요)
   - 메모: `설문 자동발급 (survey_id={uuid})` 또는 `issued_source='survey'`
   - `coupons.issued_source` 는 이미 `'manual' | 'survey'` enum 으로 준비돼있음

3. **연결 (미해결, 세션 15 의 5.2 백로그)**
   - `surveys.coupon_id UUID REFERENCES coupons(id)` 컬럼 추가 마이그레이션 필요 (세션 15 의 마이그 18 — 아직 미적용)
   - 마이그 파일: `_DEV/Seeds/21_surveys_coupon_link.sql` (신규 작성)
   - 설문 insert → 쿠폰 insert → `surveys.coupon_id` 업데이트 (2단계)
   - 또는 RPC 함수 하나로 트랜잭션 처리 (Supabase 는 단일 RPC 내 원자성 보장)

4. **손님 UX**
   - 설문 제출 완료 화면 (`SurveyForm.tsx` 의 success 상태) 에 **쿠폰 코드 크게 노출**
   - "복사하기" 버튼 + "주문하기로 이동" 버튼
   - 만료일 표시
   - 이미 `SurveyForm.tsx` 의 success 블록이 있으니 쿠폰 영역만 추가

5. **어드민 UX**
   - `AdminCoupons` 필터에 이미 `source: manual/survey` 있음 — 자동 발급분이 자동으로 노출
   - `AdminSurvey` 상세 모달에 `coupon_code` 컬럼 추가 여부?

6. **엣지 케이스**
   - 쿠폰 발급 실패 시 설문 자체는 저장되었는지?
   - (a) 설문 저장 성공 + 쿠폰 발급 실패 → 어드민에서 수동 재발급 가능
   - (b) 트랜잭션 RPC 로 묶어서 all-or-nothing
   - **(b) 추천** — 데이터 정합성 우선

7. **활성 festival 조회 lib (세션 15 의 5.2)**
   - `src/lib/festival.ts` 에 `fetchActiveFestival()` 추가
   - 현재 시점이 festival `start_date ~ end_date` 사이인 festival 반환
   - 쿠폰 만료일 계산용 (festival.end_date)

**작업 순서 (권장)**

1. 마이그 `_DEV/Seeds/21_surveys_coupon_link.sql`
   - `ALTER TABLE surveys ADD COLUMN coupon_id UUID REFERENCES coupons(id) ON DELETE SET NULL;`
   - `coupons` 의 `issued_source` 에 `survey` 값이 이미 있는지 확인 (기존 스키마에 있음)
2. Supabase RPC `issue_survey_coupon(survey_input jsonb)` — 설문 + 쿠폰 원자 insert
3. 활성 festival 조회 lib 추가
4. `src/lib/survey.ts` 의 `submitSurvey` 를 RPC 호출로 교체 → 쿠폰 코드 반환
5. `SurveyForm.tsx` success 화면에 쿠폰 코드 노출
6. 쿠폰 컴포넌트 재사용 가능하면 분리 (`CouponCodeCard.tsx`)
7. 빌드 + 더미 설문 제출 테스트 + 쿠폰 검증

**예상 변경 범위**
- 신규: 마이그 1, RPC 1, lib/festival 확장, CouponCodeCard 컴포넌트
- 수정: `src/lib/survey.ts`, `src/pages/sections/survey/SurveyForm.tsx`, `AdminSurvey` 상세 모달 (선택)

### 5.1 — 알람 묶음 (세션 11~16 백로그)
- `useWakeLock` / `audioCue` / BoothDashboard onOrderPaid + 1분 초과 alert / AdminMonitor alertCount / AdminLayout 타이틀 카운터
- 핸드오프 11/12/13/14/15/16 에 설계 누적

### 5.2 — 알리고 SMS 도입
### 5.3 — 부스 거절 → 손님 SMS
### 5.4 — 설문 통계 탭 추가 튜닝 (이번 세션에서 기본 구조는 완성 — 실제 데이터로 필드 점검 필요)

### 운영 한계 / 백로그
세션 16 과 동일.

---

## 6. 회귀 점검 체크리스트

✅ Heroicons import 0 개 (src/)
✅ 글로벌 stroke-width 0.9 적용 (BottomNav 제외 모든 아이콘)
✅ BottomNav active 상태 — primary 컬러 + lift + scale
✅ 공지사항 손님 목록 (무한스크롤) + 상세 (markdown 렌더링)
✅ 공지사항 어드민 CRUD + 이미지 업로드 + 고정/발행 토글
✅ 어드민 사이드바: 공지/신청/쿠폰/매출/만족도 순
✅ 만족도조사 탭 KPI 5개 (평균연령/최다지역 포함)
✅ 만족도조사 카드 순서 Q1 → Q20
✅ 상세 모달 질문 1줄 (3:2 그리드)
✅ 네이비 버튼 #283648 (쿠폰/공지/모니터)
✅ 어드민 모든 페이지 컬러 통일 (구 그린 흔적 0)
✅ 날짜 포맷 mm/dd hh:mm (KST)

---

## 7. 커밋 요약 (세션 17)

- `b92cfac` Migrate icons from Heroicons to Lucide
- `ee2826c` Build notice board and unify admin/booth theme to steel blue
- `77e0424` Add satisfaction survey admin tab and split dashboard into sidebar
- `3f22e7a` Tune survey KPI grid, detail modal, and navy button color

---

세션 17 끝. 아이콘/컬러/공지/만족도조사 관리 4개 축을 모두 통과. 다음 세션은 **만족도조사 대상자 자동 쿠폰 발급**. 설문 제출 → 쿠폰 자동 insert → 손님 success 화면 코드 노출 → 어드민 `AdminCoupons` 에 source=survey 로 자동 집계. 마이그 21 (`surveys.coupon_id`) + Supabase RPC (원자성) + 활성 festival lib 를 순서대로.

메모리에 저장된 규약:
- 어드민 날짜 포맷은 AdminOrders 의 `mm/dd hh:mm` (KST) 재사용
- Realtime REPLICA IDENTITY FULL (surveys 는 이미 설정됨)
