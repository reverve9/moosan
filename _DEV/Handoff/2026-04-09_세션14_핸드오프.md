# Handoff — 2026-04-09 세션 14

세션 13 다음 세션. realtime health + 헤더 back / 매장관리 토글 통일 / food_menus 실시간 + 결제전 검증 / 부스 주문 거절 + 부분 환불 / 어드민 사이드바 정리 / survey dev bypass / 부스 대시보드 태블릿 레이아웃. 알람/연결끊김배너UX고도화/쿠폰SMS/도메인 통합은 다음 세션으로.

---

## 1. 세션 14 에서 끝난 것

### A. 6.B — 연결 상태 + 자동 재연결 (`realtimeHealth` + `ConnectionBanner`)

**목적**: supabase realtime 연결이 끊겼을 때 운영자(부스/어드민)에게 즉시 알리고, 일정 시간 회복 안 되면 강제 재시도.

**구현**
- `src/lib/realtimeHealth.ts` — 싱글톤 폴링 (2s) + 2틱 debounce(=4s) → degraded → 15s 후 강제 `disconnect()+connect()` (10s cooldown)
- `src/hooks/useRealtimeHealth.ts` — React 훅 wrapper
- `src/components/ui/ConnectionBanner.tsx` + `.module.css` — 상단 fixed 빨간 bar, "다시 시도" 버튼, 회복 시 toast
- `BoothDashboardPage` 에 마운트 + 헤더 connDot 신호를 글로벌 health 로 교체
- `AdminLayout` 에 마운트 (authed 분기 안)
- `boothOrders.ts` 의 dead `onConnectionChange` 콜백 제거 (글로벌 health 로 통합)

**핵심 동작**
- supabase 의 reconnectTimer (lib 내장) 가 멈춘 경우를 우회. 채널 객체는 그대로 유지되며 `_onConnOpen()` 에서 자동 rejoin
- 폴링은 hasChannels 가 0 일 땐 무시 (앱 시작 직후 socket 미연결이 정상)

### B. 헤더 back 버튼 — 결제 결과 페이지에서만 차단

**문제**: 토스 결제는 `windowTarget: 'self'` 로 같은 윈도우 리다이렉트 → history 에 토스 도메인 잔해 → `/checkout/success` / `/checkout/fail` / `/order/:id` (checkout 진입) 에서 back 누르면 토스 401 페이지로 튕김.

**해결 (옵션 3 — 결제 결과만 차단)**
- `routing.ts` 에 `isNoBackPage(pathname)` 추가 — `/checkout/success`, `/checkout/fail` 무조건 차단
- `Header.tsx` — `subPage && !noBack` 조건. `/order/:id` 는 `?from=checkout` 쿼리가 있을 때만 차단
- `CheckoutSuccessPage` — `navigate('/order/:id?from=checkout', { replace: true })` + Link 도 동일 쿼리 (history.state 대신 URL query 사용 — 새로고침/탭 복원/process kill 모두 안전)
- 효과:
  - 결제 결과 페이지: back 버튼 숨김, 페이지 안 CTA 만 사용
  - `/order/:id` ← Checkout 진입: back 숨김
  - `/order/:id` ← CartPage "내 주문" 진입: back 정상 (`/cart` 로 돌아감)

### C. 매장관리 토글 방향 통일

**문제**: 영업 / 준비 중 / 메뉴 품절 토글이 knob 위치 + 색이 서로 달라 부스 운영자가 헷갈림. 준비 중 토글은 statusToggleOn / statusTogglePaused 둘 다 `transform: translateX(98px)` 라 knob 자체가 안 움직이는 버그까지.

**통일 (모든 토글: right=ON=정상=녹색, left=OFF=비활성=경고색)**
- 영업: right=영업 중(녹), left=영업 종료(빨강) — 그대로
- 준비 중: statusTogglePaused 에서 knob transform 제거 → right=주문 가능(녹), left=준비 중(주황)
- 메뉴: toggleOn 색 빨강 → 녹색. tsx 의 클래스 적용 조건 `soldOut` → `!soldOut`. aria-pressed/aria-label 같이 반전 (라벨 텍스트 "품절"/"판매중" 은 그대로)

### D. food_menus 실시간 + 결제 직전 sold-out 검증

**문제**: `food_menus` 가 supabase realtime publication 에 없어서 부스가 품절 토글해도 손님 페이지 새로고침 전까지 정상 결제 가능.

**해결 (1차 + 2차)**
- **1차 — 마이그 16** (`_DEV/Seeds/16_food_menus_realtime.sql`)
  - publication 에 `food_menus` 추가 + `REPLICA IDENTITY FULL`
- **1차 — FoodSections 구독**
  - 새 채널 `food-menus-status` (UPDATE 전체 구독, 클라이언트에서 booth_id 매칭으로 필터)
  - payload.new 의 is_sold_out / is_active / price 를 booths.menus 배열에서 in-place 패치
- **2차 — CheckoutPage 결제전 검증**
  - createPendingPayment 호출 직전 `supabase.from('food_menus').select('id, name, is_sold_out, is_active').in('id', menuIds)` → 품절/비활성 메뉴 있으면 abort + 토스트
  - localStorage 복원 / 다른 탭 등 realtime 구독 못 받은 경우 안전망

### E. 부스 단위 주문 거절 + 부분 환불 시스템 (큰 묶음)

**목적**: 결제 후에도 부스가 매장 사정(재료 소진/조리 불가/손님 요청)으로 자기 부스 주문만 거절 가능. 토스 부분환불 + 클라이언트/어드민/부스 모두 realtime 반영.

**핵심 결정**
| 항목 | 결정 |
|---|---|
| 환불 단위 | order 1건 (부분환불) |
| 거절 가능 시점 | order.status in ('paid','confirmed') AND ready_at IS NULL |
| 환불액 | `min(order.subtotal, payment.total_amount - refunded_amount)`. 쿠폰 할인은 운영자 부담이라 영업점은 비례 분배 없이 subtotal 그대로 환불 |
| 사유 입력 | 강제 + dropdown(재료 소진/조리 불가/손님 요청/기타) |
| 손님 알림 | OrderStatusPage realtime 구독 (이미 있음). SMS 는 6.C 와 같이 |
| API | 신규 `api/orders/cancel.ts` (어드민 풀환불 endpoint 와 분리) |

**마이그 17** (`_DEV/Seeds/17_order_rejection.sql`)
- `orders` 에 `cancelled_at`, `cancel_reason`, `cancelled_by` ('booth'/'admin') 추가
- `payments.refunded_amount INTEGER NOT NULL DEFAULT 0` 누적 컬럼 추가
- publication / replica identity 점검 (orders/payments)

**API**
- 신규 `api/orders/cancel.ts` — orderId+reason → 검증 → Toss `cancelAmount` → DB (orders cancelled / payments.refunded_amount += / 누적이 total 도달 시 payments cancelled)
- `api/payments/cancel.ts` 수정 — 부분환불 인지: cancelled order 무시, 잔액만 환불, cancelled_by='admin'

**Client lib**
- `boothOrders.ts` 에 `cancelBoothOrder(orderId, reason)` 추가
- `adminPayments.ts` 의 `isRefundable` 부분환불 인지 + `remainingRefundable` 추가
- `adminStats.ts` 의 모든 통계 함수 (KPI/시간/부스/메뉴/고객/객단가) 가 `total_amount - refunded_amount` 사용 + cancelled order 제외

**UI**
- 신규 `BoothCancelOrderModal.tsx/.module.css` — 사유 dropdown 모달
- `BoothDashboardPage` 카드에 빨간 "거절" 버튼 + 모달 + handleCancelConfirm + actionReject 스타일
- `OrderStatusPage`:
  - 새 UI status `'partial'` 추가 (일부 부스만 cancelled)
  - 부스 카드 단위 cancelled 표시 (빨간 보더, 거절 사유 박스, 환불금액 노출, item 행 회색+취소선)
  - 결제 정보에 환불금액 줄 추가
- `AdminOrders`:
  - 목록에 "부분취소" 배지
  - DetailModal 에 환불금액/잔액/거절 사유 라인
  - 환불 안내 "잔액 N원 환불" 로 변경
  - totals 에 환불 차감 + 환불 박스 추가
- `types/database.ts` — payments.refunded_amount, orders.cancelled_at/cancel_reason/cancelled_by 추가
- 운영 시나리오 5종 별도 마크다운: `_DEV/Handoff/2026-04-09_부스주문거절_시나리오.md`

### F. 어드민 사이드바 정리

- AdminLayout 사이드바 하단 "사이트로 이동" 버튼 제거 (필요 시 새 탭 직접 진입)

### G. 만족도조사 dev bypass

- 4 step 모두 next/submit 버튼 disabled 조건에 `&& !import.meta.env.DEV` 추가
- Vite 가 production 빌드 시 false 로 inline → 운영엔 영향 없음
- step1 의 기본 정보 (gender/age/region/name/phone/privacy_consented) 만 채우면 끝까지 통과 가능. q1~q20 답 비워도 step 진행 OK
- 단, 실제 submit 시 DB NOT NULL 컬럼 (gender/age/region/name/phone/privacy_consented) 은 채워야 row 들어감

### H. 부스 대시보드 태블릿 레이아웃 (3열→2열 + minmax + fixed inset)

**문제 1**: 갤럭시탭에서 3열 카드 그리드가 좁음 → 2열로 변경
**문제 2**: 카드가 5개 이상이면 grid `1fr` 가 콘텐츠 따라 자라며 우측 매출 박스(120px)가 viewport 밖으로 밀려 잘림
**문제 3**: 안드로이드 standalone PWA 가로 모드에서 `100vh` 가 viewport 보다 큰 값으로 잡혀 페이지 자체 스크롤 + 매출 박스 아래 빈 여백

**해결**
- `.cardList`: `repeat(3, 1fr)` → `repeat(2, minmax(0, 1fr))`
- `.container`: `width: 100vw; height: 100vh` → `position: fixed; inset: 0` (100vh 부정확 우회)
- `.content`: `1fr 360px` → `minmax(0, 1fr) 360px`
- `.rightPanel`: `1fr 120px` → `minmax(0, 1fr) 120px`
- 결과: 좌측 cardList 만 자체 스크롤, 우측 completedList 만 자체 스크롤, 매출 박스(120px) 항상 viewport 하단 고정

---

## 2. 핵심 결정사항

| 항목 | 결정 |
|---|---|
| **realtimeHealth 트리거** | 폴링 2s + 2틱 debounce + 15s 후 강제 disconnect/connect |
| **결제 결과 back 마킹** | `?from=checkout` URL query (history.state 대신 — 새로고침/process kill 안전) |
| **부스 거절 환불액** | `min(subtotal, remaining)` — 쿠폰 할인은 운영자 부담, 영업점 비례 분배 X |
| **부스 거절 가능 시점** | paid/confirmed AND ready_at IS NULL |
| **거절 사유** | 강제 + dropdown 4종 (재료 소진/조리 불가/손님 요청/기타) |
| **부스 거절 API 분리** | 신규 `api/orders/cancel.ts` (어드민 풀환불은 기존 `api/payments/cancel.ts`) |
| **food_menus realtime + 결제전 검증** | 두 갈래 모두 적용 — 1차 UI 즉시 반영, 2차 결제 직전 안전망 |
| **부스 대시보드 viewport 처리** | `position: fixed; inset: 0` — 100vh 부정확 우회 |
| **매장관리 토글 방향** | right=ON=정상=녹색, left=OFF=비활성=경고색 (영업/준비/메뉴 통일) |
| **survey dev bypass** | `import.meta.env.DEV` (production 영향 0) |

---

## 3. 변경 파일

### 신규
- `_DEV/Seeds/16_food_menus_realtime.sql`
- `_DEV/Seeds/17_order_rejection.sql`
- `_DEV/Handoff/2026-04-09_부스주문거절_시나리오.md`
- `_DEV/Handoff/2026-04-09_세션14_핸드오프.md` (이 파일)
- `api/orders/cancel.ts`
- `src/lib/realtimeHealth.ts`
- `src/hooks/useRealtimeHealth.ts`
- `src/components/ui/ConnectionBanner.tsx` + `.module.css`
- `src/components/booth/BoothCancelOrderModal.tsx` + `.module.css`

### 수정
- `api/payments/cancel.ts` — 부분환불 인지 (cancelled order 무시 + 잔액 환불 + cancelled_by='admin')
- `src/types/database.ts` — payments.refunded_amount, orders.cancelled_at/cancel_reason/cancelled_by
- `src/lib/routing.ts` — `isNoBackPage` 추가
- `src/lib/boothOrders.ts` — `cancelBoothOrder` 추가, dead `onConnectionChange` 제거
- `src/lib/adminPayments.ts` — `isRefundable` 부분환불 인지 + `remainingRefundable`
- `src/lib/adminStats.ts` — KPI/시간/부스/메뉴/고객/객단가 모두 refunded_amount 차감 + cancelled 제외
- `src/components/layout/Header.tsx` — `?from=checkout` 인지, `subPage && !noBack` 조건
- `src/components/admin/AdminLayout.tsx` — ConnectionBanner 마운트, "사이트로 이동" 버튼 제거
- `src/components/booth/BoothMenuModal.tsx` + `.module.css` — 토글 방향 통일
- `src/components/food/FoodSections.tsx` — `food-menus-status` 채널 구독 추가
- `src/pages/CheckoutPage.tsx` — handleSubmit 결제 직전 sold-out 검증
- `src/pages/CheckoutSuccessPage.tsx` — `?from=checkout` 쿼리 navigate/Link
- `src/pages/OrderStatusPage.tsx` + `.module.css` — partial UI status, 부스 cancelled 카드, 환불금액
- `src/pages/admin/AdminOrders.tsx` + `.module.css` — 부분취소 배지, 잔액 환불 안내, 거절 사유 라인, totals
- `src/pages/booth/BoothDashboardPage.tsx` — ConnectionBanner 마운트, useRealtimeHealth, 거절 버튼/모달/handleCancelConfirm
- `src/pages/booth/BoothDashboardPage.module.css` — 2열 그리드, minmax, fixed inset, actionReject
- `src/pages/sections/survey/SurveyStep1Basic.tsx` ~ `SurveyStep4Opinion.tsx` — dev bypass 4곳

---

## 4. 마이그레이션 적용 체크리스트

### 마이그 16 (`_DEV/Seeds/16_food_menus_realtime.sql`) — **이미 사용자가 적용함**
검증:
```sql
SELECT pubname FROM pg_publication_tables WHERE tablename = 'food_menus';
-- → supabase_realtime
SELECT relreplident FROM pg_class WHERE relname = 'food_menus';
-- → 'f' (FULL)
```

### 마이그 17 (`_DEV/Seeds/17_order_rejection.sql`) — **이미 사용자가 적용함**
검증:
```sql
SELECT column_name FROM information_schema.columns
WHERE table_name='orders' AND column_name IN ('cancelled_at','cancel_reason','cancelled_by');
-- → 3 rows
SELECT column_name FROM information_schema.columns
WHERE table_name='payments' AND column_name='refunded_amount';
-- → 1 row
SELECT relreplident FROM pg_class WHERE relname IN ('orders','payments');
-- → 'f' 두 행
```

---

## 5. 미해결 작업 / 의도적 제외

### **🔥 다음 세션 0순위 — 도메인 연결 + 버셀 배포 통합**
- 운영 도메인 (manhaemusan.or.kr 또는 별도 도메인) 연결
- Vercel 3 → 1 프로젝트 통합 — 손님/부스/어드민 host 분기 구조는 이미 `App.tsx:46-54` 에 있음
- DNS 설정, SSL, www 처리, redirect 정책
- 통합 후 PWA 매니페스트 / orientation 동작 재확인
- 핸드오프 12/13 에 "운영 도메인 확정 후 5분 작업" 으로 미뤄둔 항목

### 6.A 알람 묶음 (핸드오프 11/12/13 부터 미진행)
- `useWakeLock` — `navigator.wakeLock.request('screen')` + visibilitychange 재요청
- `audioCue` — OscillatorNode 싱글톤 + `playSound('newOrder' | 'alert')`
- BoothDashboard onOrderPaid → audio + `navigator.vibrate([200,100,200])`
- BoothDashboard 1분 초과 전이 훅 → 1회 alert sound
- AdminMonitor alertCount 전이 훅 → sound
- AdminLayout `monitorPending > 0` → `document.title = '(N) ...'`, unmount 복구

### 6.C 설문 → 쿠폰 자동 발급 + SMS
- 마이그 18: `surveys.coupon_id UUID REFERENCES coupons(id)` 추가
- 쿠폰 정책 결정 (사용자 협의): 금액 / 만료 / 최소주문
- `lib/survey.ts` `submitSurvey` 가 insert 후 `createCouponManually` 호출 + `surveys.coupon_id` 업데이트
- `SurveyForm` 완료 화면에 쿠폰 코드 박스 + 복사 버튼
- **활성 festival 조회 lib** 추가 → festivalId 자동 주입 (createPendingPayment / submitSurvey 모두)
- **SMS 알리고** — `@aligoapi/aligoapi`. `api/coupons/send-sms.ts` 신규
- SMS 실패해도 쿠폰은 DB 에 있으므로 어드민에서 코드 재전달 가능

### 6.D 설문 통계 탭 (어드민)
- AdminDashboard 에 "설문 통계" 탭 추가 (탭 구조 이미 있음)
- 응답 수 / 응답률 / Q11 만족도 / Q17 추천 의향 분포

### 부스 거절 → 손님 SMS 알림 (6.C 와 같이 연계)
- 거절 시 손님에게 "[부스명] 주문이 거절되어 N원 환불 처리됐습니다" SMS
- 알리고 도입 후 `api/orders/cancel.ts` 안에 SMS 호출 추가

### 운영 한계 / 백로그
| 항목 | 상태 | 비고 |
|---|---|---|
| **부스/admin cancel 트랜잭션** | 미해결 | Toss 환불 성공 후 DB UPDATE 실패 시 잔액 불일치 가능. 운영 중 발생하면 RPC 로 묶기 |
| **food_menus.is_sold_out 손님측 Realtime** | ✅ 이번 세션 완료 | (마이그 16) |
| **활성 festival_id 자동 주입** | 미진행 | 6.C 와 같이 |
| **클라이언트 콘텐츠 페이지 px → 토큰 정리 (Phase 3)** | 미진행 | 시급도 낮음 |
| **PWA 2단계 Web Push** | 미진행 | 1단계 설치 UI 끝 |
| **CSV 익스포트** | 미진행 | 어드민 백로그 |
| **부분 환불** | ✅ 이번 세션 완료 |
| **설문 응답 중간 저장** | 의도적 제외 | localStorage/draft 없음 |

---

## 6. 다음 세션 작업 추천 순서

### 6.0 Pre-flight
- `git pull origin main` (최신 `7177879`)
- `npm install` (의존성 변경 없음)
- 마이그 16, 17 적용 확인
- `vercel dev` (booth.localhost / admin.localhost / localhost)

### 6.1 — 도메인 연결 + 버셀 배포 통합 (**0순위, 사용자 명시**)
1. 운영 도메인 확정
2. Vercel 3 프로젝트 → 1 프로젝트로 통합 (deploy 단일화)
3. DNS / SSL / www / redirect 정책
4. App.tsx 의 hostname 분기 (`booth.* / admin.* / 그 외`) 가 새 도메인에서 정상 동작 확인
5. PWA manifest (특히 부스 가로 강제 inline script) 가 새 도메인에서 정상 동작 확인
6. 부스 PWA 재설치 후 가로 모드 고정 + ConnectionBanner 동작 확인

### 6.2 — 알람 묶음 (6.A)
- 핸드오프 11/12/13 에 이미 설계 적힘. 코드 바로 들어감

### 6.3 — 설문 → 쿠폰 + SMS (6.C)
- 마이그 18 → 쿠폰 정책 결정 → submitSurvey 확장 → 활성 festival 조회 lib → 알리고 SMS

### 6.4 — 설문 통계 탭 (6.D)
- AdminDashboard 새 탭

### 6.5 — 부스 거절 → 손님 SMS (6.C 직후)
- `api/orders/cancel.ts` 안에 SMS 호출 추가

---

## 7. 운영 테스트 시나리오

부스 주문 거절 + 부분환불은 별도 마크다운 참조: `_DEV/Handoff/2026-04-09_부스주문거절_시나리오.md` (5종 시나리오 + 회귀 점검)

기타 회귀:
- 손님 결제 흐름 (정상 / 카트→체크아웃→토스→success)
- 헤더 back 버튼 (`/checkout/success`, `/checkout/fail`, `/order/:id?from=checkout` 에서 숨김)
- 매장관리 토글 3종 모두 right=ON=정상
- 갤럭시탭 부스 PWA 가로 강제 + 2열 그리드 + 매출 박스 viewport 하단 고정 + 좌/우 자체 스크롤
- realtime 끊김 시뮬레이션 (wifi off) → 4초 후 빨간 배너 → 복구 시 toast
- 만족도조사 dev bypass — 빈 값 step 진행 OK, submit 은 DB constraint 에서 거부

---

## 8. 머신 시작 체크리스트

```bash
# 1. 최신 main
git pull origin main
# 최신: 7177879 "Pin booth dashboard container with fixed inset"

# 2. 의존성 (변경 없음)
npm install

# 3. env 확인 — VITE_SUPABASE_URL / VITE_SUPABASE_ANON_KEY / VITE_TOSS_CLIENT_KEY / TOSS_SECRET_KEY

# 4. 마이그 적용
#    07~15 + 16_food_menus_realtime.sql + 17_order_rejection.sql

# 5. 빌드
npm run build

# 6. 로컬 dev
vercel dev   # 결제/쿠폰/주문거절 API 위해 vercel dev 권장
# http://localhost:3000/            ← 손님
# http://booth.localhost:3000/      ← 부스
# http://admin.localhost:3000/      ← 어드민

# 7. 다음 작업 — 도메인 연결 + Vercel 통합 (0순위)
```

---

## 9. 커밋 요약 (이 세션)

- `489ae38` Add realtime health monitor with reconnect banner
- `2bb57a5` Hide header back button on checkout result pages
- `6b8dc91` Sync food_menus sold-out to customer in realtime
- `505c7f7` Align booth manage toggles to a single direction
- `8eed100` Add booth order rejection with partial refund
- `cf2b23d` Remove site-link button from admin sidebar
- `2dd823a` Bypass survey step validation in dev
- `e9d6512` Fix booth dashboard layout for tablet (2-col + minmax)
- `7177879` Pin booth dashboard container with fixed inset

모두 `main` 푸시 완료.

---

세션 14 끝. 큰 묶음 — realtime health / 헤더 back / 토글 통일 / food_menus 실시간 + 결제전 검증 / 부스 거절 + 부분환불 / 어드민 사이드바 정리 / survey dev bypass / 부스 태블릿 레이아웃. 부스 거절 + 부분환불이 가장 큼 (3 commit 묶음 분량).

다음 세션 0순위는 **도메인 연결 + Vercel 통합**. 그 이후 알람 / 쿠폰 SMS / 설문 통계.

고생 많으셨어요. 내일 봬요.
