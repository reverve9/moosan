# Handoff — 2026-04-10 세션 23

세션 22 (코드베이스 분석 + 클린업 + 스트리트 폼 재구성) 이후. **세션 23: 프롬프트 v9 구현 — 예상 조리시간 선택 + 초과 모니터링 + avg_prep_minutes 제거.**

---

## 1. 세션 23 에서 끝난 것

### A. 주문 확인 → 예상 조리시간 선택 버튼으로 변경
- 기존 "확인" 단일 버튼 제거
- 5개 써클 버튼(5/10/15/20/30) 으로 대체 — 숫자만, 네이비(#283648), 5열 그리드
- 버튼 클릭 시 `confirmed_at` + `estimated_minutes` 동시 업데이트
- 카드 footer 2줄 구조: 1줄(금액 + 거절/조리완료), 2줄(시간 써클 그리드, waiting만)

### B. 카운트다운 타이머
- 확인 후 경과시간 표시 → 카운트다운으로 변경
- 예: 5분 선택 → `4:59` → `0:00` → `+0:15` (빨강 오버타임)
- waiting 상태는 기존 elapsed 유지 (`1분 30초 전`)

### C. 손님 OrderStatusPage 예상 시간 표시
- 매장 확인 후: "매장 확인완료 · 약 10분 후 준비됩니다"
- 상단 상태 카드: "조리 중 · 약 10분 후 준비됩니다"
- estimated_minutes 없으면 기존 문구 유지

### D. 조리시간 초과 모니터링 (AdminMonitor)
- `confirmed_at + estimated_minutes + 1분` 초과 시 감지
- 스탯 박스 4번째 추가: "조리 초과" (파랑 #3B82F6 계열)
- 파랑 배너: "조리시간 초과 N건"
- 부스 카드: "⏰ 조리시간 초과" 배지
- AdminLayout 사이드바 배지에 overdueCount 반영 (파랑)

### E. 부스앱 조리시간 초과 알람
- `estimated_minutes + 2분` 초과 시 alarm 1회 재생 + 진동
- `overdueAlertedIds` Set으로 주문별 1회만

### F. avg_prep_minutes 완전 제거
- DB: `food_booths.avg_prep_minutes` 컬럼 DROP
- 코드 제거: AdminFood 편집 필드, waiting.ts(`calcWaitingInfo`, `fetchBoothWaitingSummariesByIds`), FoodSections 모달, CheckoutPage 대기 표시
- 대기 배지: 건수만 표시 (여유 / 대기 N건 / 혼잡 N건), 시간 제거
- CheckoutPage: `fetchAllBoothWaitingCounts` + `getBoothBadge` 로 전환

### G. 완료 카드 날짜 포맷
- 기존: `18:28` (시간만)
- 변경: `04/10 18:28` (MM/DD HH:MM)

---

## 2. DB 마이그레이션

| SQL 파일 | 상태 |
|----------|------|
| `_DEV/Seeds/24_add_estimated_minutes_drop_avg_prep.sql` | ✅ 실행 완료 |

```sql
ALTER TABLE orders ADD COLUMN IF NOT EXISTS estimated_minutes INTEGER;
ALTER TABLE food_booths DROP COLUMN IF EXISTS avg_prep_minutes;
```

---

## 3. 커밋 (세션 23, dev 브랜치)

- `ceac855` — v9 메인 구현: 시간 선택 + 초과 모니터링 + avg_prep_minutes 제거
- `6e4a69e` — 시간 버튼 네이비 색상 + 높이 정렬
- `3df24ef` — 조리완료 버튼 원래 구조 복원
- `7bf637b` — footer 2줄 + 시간 그리드 + 카운트다운 + 완료 날짜
- `d2ea601` — 써클 + 큰 폰트 + gap + MM/DD HH:MM 포맷
- `c61fe3e` — 써클 사이즈/폰트 미세 조정
- `00099eb` — main 머지 (ceac855 + 6e4a69e 시점)

**주의:** main에는 `00099eb`까지만 머지됨 (중간 시점). 최종본(`c61fe3e`)은 dev에만 있음. 테스트 후 main 머지 필요.

---

## 4. 변경 파일 목록

| 파일 | 변경 |
|------|------|
| `src/types/database.ts` | orders에 estimated_minutes 추가, food_booths에서 avg_prep_minutes 제거 |
| `src/types/festival_extras.ts` | avg_prep_minutes 제거 |
| `src/lib/boothOrders.ts` | confirmBoothOrder에 estimatedMinutes 파라미터 추가 |
| `src/lib/boothMonitor.ts` | MonitorConfirmedRow 추가, fetchMonitorSummary → MonitorData 반환 |
| `src/lib/waiting.ts` | calcWaitingInfo/fetchBoothWaitingSummariesByIds 제거, getBoothBadge 임계값 조정 |
| `src/pages/booth/BoothDashboardPage.tsx` | 시간 써클 그리드, 카운트다운, 초과 알람, 날짜 포맷 |
| `src/pages/booth/BoothDashboardPage.module.css` | timeGrid/timeGridBtn, footer 2줄 구조 |
| `src/pages/OrderStatusPage.tsx` | 예상 시간 표시 (boothStatusLabel, getStatusLabel) |
| `src/components/admin/AdminAlertContext.tsx` | overdueCount, confirmedOrders 추가 |
| `src/pages/admin/AdminMonitor.tsx` | 4번째 스탯, 파랑 배너, 초과 배지 |
| `src/pages/admin/AdminMonitor.module.css` | overdue 스타일 (파랑 계열) |
| `src/components/admin/AdminLayout.tsx` | 사이드바 배지 overdueCount 반영 |
| `src/components/admin/AdminLayout.module.css` | navBadgeOverdue 추가 |
| `src/pages/admin/AdminFood.tsx` | avg_prep_minutes 편집 필드 제거 |
| `src/components/food/FoodSections.tsx` | calcWaitingInfo → getBoothBadge |
| `src/pages/CheckoutPage.tsx` | fetchBoothWaitingSummariesByIds → fetchAllBoothWaitingCounts |

---

## 5. 빌드 결과

| 항목 | 값 |
|------|-----|
| 타입체크 | ✅ 통과 |
| 빌드 | ✅ 통과 |
| index.css | 196.80 KB (gzip 31.97 KB) |
| xlsx chunk | 424.79 KB (gzip 141.50 KB) |
| index.js | 795.51 KB (gzip 221.90 KB) |

---

## 6. 머신 시작 체크리스트

```bash
# 1. dev 브랜치
git pull origin dev
# 커밋: c61fe3e

# 2. 의존성
npm install

# 3. DB — 실행 완료 (세션 23에서 적용됨)
#    _DEV/Seeds/24_add_estimated_minutes_drop_avg_prep.sql

# 4. 빌드
npm run build

# 5. 로컬 dev
vercel dev
```

---

## 7. 미해결 / 다음 세션

### main 머지 필요
dev(`c61fe3e`)가 main보다 앞서 있음. 테스트 완료 후 main 머지 필요.

### Vercel 배포 이슈
dev 브랜치 프리뷰 배포 상태 확인 필요. main 자동 배포도 간헐적 이슈 (세션 22 이월).

---

## 8. 회귀 점검

✅ 빌드 통과 (`npm run build`)
✅ 타입체크 통과 (`tsc -b`)
✅ avg_prep_minutes 코드 참조 0건
✅ 기존 조리완료 버튼 유지
✅ 준비완료 스트립 (v8) 정상
